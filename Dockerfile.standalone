FROM node:20-alpine

WORKDIR /app

COPY package*.json tsconfig.json ./

RUN npm ci

COPY src ./src

RUN npm run build

RUN npm prune --omit=dev

EXPOSE 3000

ENV TRANSPORT=stdio

CMD ["node", "dist/standalone.js"]
